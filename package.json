{
  "name": "winreg",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "node-gyp --debug configure build",
    "build": "node-gyp build",
    "joytest": "node-gyp rebuild --arch=x64",
    "joytest32": "node-gyp rebuild --arch=ia32"
  },
  "dependencies": {
    "node-addon-api": "^3.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.0.1"
  }
}
